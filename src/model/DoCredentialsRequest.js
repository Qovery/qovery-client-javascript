/**
 * Qovery API
 * - Qovery is the fastest way to deploy your full-stack apps on any Cloud provider. - ℹ️ The API is stable and still in development. 
 *
 * The version of the OpenAPI document: 1.0.3
 * Contact: support+api+documentation@qovery.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The DoCredentialsRequest model module.
 * @module model/DoCredentialsRequest
 * @version $(grep &#39;version&#39; _build/openapi.yaml | head -1 | tr &#39;:&#39; &#39;\n&#39; | tail -1 | tr -d &#39; &#39;)
 */
class DoCredentialsRequest {
    /**
     * Constructs a new <code>DoCredentialsRequest</code>.
     * @alias module:model/DoCredentialsRequest
     * @param name {String} 
     */
    constructor(name) { 
        
        DoCredentialsRequest.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>DoCredentialsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DoCredentialsRequest} obj Optional instance to populate.
     * @return {module:model/DoCredentialsRequest} The populated <code>DoCredentialsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DoCredentialsRequest();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('spaces_access_id')) {
                obj['spaces_access_id'] = ApiClient.convertToType(data['spaces_access_id'], 'String');
            }
            if (data.hasOwnProperty('spaces_secret_key')) {
                obj['spaces_secret_key'] = ApiClient.convertToType(data['spaces_secret_key'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DoCredentialsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DoCredentialsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DoCredentialsRequest.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['spaces_access_id'] && !(typeof data['spaces_access_id'] === 'string' || data['spaces_access_id'] instanceof String)) {
            throw new Error("Expected the field `spaces_access_id` to be a primitive type in the JSON string but got " + data['spaces_access_id']);
        }
        // ensure the json data is a string
        if (data['spaces_secret_key'] && !(typeof data['spaces_secret_key'] === 'string' || data['spaces_secret_key'] instanceof String)) {
            throw new Error("Expected the field `spaces_secret_key` to be a primitive type in the JSON string but got " + data['spaces_secret_key']);
        }

        return true;
    }


}

DoCredentialsRequest.RequiredProperties = ["name"];

/**
 * @member {String} name
 */
DoCredentialsRequest.prototype['name'] = undefined;

/**
 * @member {String} token
 */
DoCredentialsRequest.prototype['token'] = undefined;

/**
 * @member {String} spaces_access_id
 */
DoCredentialsRequest.prototype['spaces_access_id'] = undefined;

/**
 * @member {String} spaces_secret_key
 */
DoCredentialsRequest.prototype['spaces_secret_key'] = undefined;






export default DoCredentialsRequest;

